document.querySelectorAll('.download-btn').forEach(button => {
    button.addEventListener('click', function() {
        alert('¡Gracias por descargar! El archivo comenzará a descargarse ahora.');
    });
});